<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="@dimen/activity_horizontal_margin_small"
    android:paddingTop="@dimen/activity_vertical_margin_small"
    >

    <TextView
        android:id="@+id/template_header_textview"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/template_header_text"
        android:textSize="@dimen/big_text"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/template_header_imagebutton"
        app:layout_constraintBottom_toBottomOf="@+id/template_header_imagebutton"
        app:layout_constraintEnd_toStartOf="@+id/template_header_imagebutton"
        />

    <ImageButton
        android:id="@+id/template_header_imagebutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/ic_new_icon"
        />

</androidx.constraintlayout.widget.ConstraintLayout>